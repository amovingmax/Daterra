================================================================
CHANGELOG — branch feature/2 (máquina rafael@rafaelabreu.com)
Changelog para o funilamento (3º Claude) entender o que foi feito.
Uma entrada por commit, da mais recente pra mais antiga.
================================================================


----------------------------------------------------------------
[2026-06-02] Conformidade App Store — itens 6 e 7
----------------------------------------------------------------
Contexto: divisão de tarefas da lista de bloqueadores da App Store.
Esta máquina (feature/2) ficou com os itens 6 (conta demo) e 7 (iPad).
A outra máquina (feature/1) ficou com o item 2 (identidade do app no app.json).

O QUE FOI FEITO:

1) ITEM 7 — iPad (app.json)
   Arquivo: apps/mobile/app.json
   Mudança: ios.supportsTablet  true -> false
   Motivo: supportsTablet=true obriga screenshots de iPad + app funcional
   em iPad (Apple testa). App é mobile-first/portrait; tirar iPad agora
   remove essa exigência e reduz risco de rejeição. Reativável no futuro.

2) ITEM 6 — Conta de demonstração para revisores
   a) NOVO: packages/database/scripts/seed-demo-account.mjs
      Script idempotente que cria/atualiza a conta demo via service-role.
      Aproveita o trigger on_auth_user_created (cria o profile sozinho a
      partir do user_metadata full_name/phone).
      Credenciais: revisor.appstore@daterra.app / DaTerra#Review2026
      Requer env: SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY (NÃO commitar).
      Ainda NÃO foi executado (faltava conectar o Supabase na hora).
   b) NOVO: apps/mobile/STORE_REVIEW_NOTES.md
      Rascunho das notas de revisão p/ colar no App Store Connect
      (Sign-In Information + Notes), explicando modo visitante, pagamento
      de bem físico sem IAP (3.1.3e) e exclusão de conta (5.1.1v).

INFRA (não entra no commit, mas registrado p/ contexto):
   - CLI do Supabase linkado ao projeto "Daterra" (ref sbqjkqcbupvzktrkunmf).
     O estado fica em packages/database/supabase/.temp (gitignored).

⚠️ PONTOS DE COLISÃO PARA O FUNILAMENTO:
   - apps/mobile/app.json: ESTA branch alterou o bloco "ios"
     (supportsTablet). A feature/1 (item 2) também mexe no app.json
     (name, bundleIdentifier, version, buildNumber). Conflito de merge
     esperado nesse arquivo — resolver mantendo AMBAS as mudanças
     (supportsTablet:false + os campos de identidade).

⚠️ PENDÊNCIAS QUE AFETAM O ITEM 6 (não resolvidas aqui):
   - Item 5 (pagamento simulado): enquanto o checkout não cobrar de verdade,
     a nota de "PAGAMENTO" nas review notes fica inconsistente com o app.
   - Item 4 (política de privacidade hospedada): precisa de URL antes de
     submeter.
