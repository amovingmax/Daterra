auto-install-peers=true
strict-peer-dependencies=false
# Expo precisa de node-linker=hoisted (configurado em pnpm-workspace.yaml).
# Next.js funciona bem com hoisted também — não precisamos override por app.
shamefully-hoist=true
